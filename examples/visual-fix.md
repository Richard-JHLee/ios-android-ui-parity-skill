# Example: Fixing Visual Differences

```text
Use the iOS to Android UI Parity Skill.
Compare the attached iOS reference screenshot and Android screenshot.
Do not redesign the screen.
Fix only layout hierarchy, component position, spacing, typography, dimensions, icon alignment, chart/gauge constraints, clipping, overflow, and system inset differences.
Do not change API behavior or business logic.
After changes, capture a new screenshot and compare again.
```
